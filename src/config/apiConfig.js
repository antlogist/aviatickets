/**
 * @constant {object}
 * @description The object contains the URL that provided access to the Aviasales API.
 * @property {string} url - The Aviasales API URL
 */
const config = {
    url: "https://aviasales-api.herokuapp.com"
};

/**
 * The config object
 */
export default config;
